# ai-bid 智能标书审核系统 — 新电脑运行指南

> 本文档基于 Windows 11 实际部署验证编写。所有命令在 **PowerShell** 中执行。
> 文档中的"坑点"均为真实部署中遇到的问题，按顺序操作可避开。

---

## 零、从压缩包部署（新电脑第一步）

源码以压缩包方式传递：**`ai-bid-source.zip`（19MB）**，解压到新电脑任意目录。

**压缩包已排除的目录**（都是可再生产物，不复制）：

| 已排除目录 | 大小 | 新电脑如何处理 |
|---|---|---|
| `backend-rust/target/` | 4.7G | 首次 `cargo run` 自动重新编译（5-10 分钟） |
| `frontend/node_modules/` | 496M | `pnpm install` 重新安装 |
| `backend-java/target/` | 72M | `mvn clean package -DskipTests` 重新编译 |
| `output/`、`tmp/` | 90M | 运行数据，不需要 |

**压缩包完整保留**：全部源码、测试 PDF（`backend-rust\tests\file\`）、SQL 建表脚本、`.env`（API Key 为空，需填）、`运行指南.md`。

> 因此解压后，**第 4、5、6 步的编译/安装命令是必须执行的**，不能跳过。下面相应步骤已标注 ✅必须。

---

## 一、环境要求

| 工具 | 版本要求 | 验证命令 |
|---|---|---|
| Docker Desktop | 24+（含 Compose v2） | `docker --version` |
| Node.js | 18+ | `node --version` |
| pnpm | 8+ | `pnpm --version` |
| JDK | 17+（推荐 21 LTS） | `java -version` |
| Maven | 3.8+ | `mvn --version` |
| Rust | 1.80+ | `rustc --version` |
| Python | 3.x | `python --version` |

> 前五项缺失会导致对应服务无法启动，务必先装齐。Maven 下载 **Binary zip archive**（不是 Source）。

---

## 二、环境安装要点

### 1. Docker Desktop
- 官网下载：<https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe>
- 安装勾选 **Use WSL 2**，装完重启电脑
- **国内镜像加速（重要）**：编辑 `C:\Users\<用户名>\.docker\daemon.json`：

```json
{
  "experimental": false,
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1ms.run",
    "https://docker-0.unsee.tech"
  ]
}
```

  然后**重启 Docker Desktop** 才生效。配置对 Docker Hub 镜像有效，对 `ghcr.io` 无效（见坑点 1）。

### 2. JDK + Maven
- JDK：Adoptium Temurin 21（<https://adoptium.net/download/>），装完自动配好 `JAVA_HOME`
- Maven：<https://maven.apache.org/download.cgi> 下 Binary zip，解压到 `C:\tools\apache-maven-x.x.x`，把 `bin` 目录加入系统 PATH

### 3. Rust
```powershell
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustup default stable
```

### 4. Node.js + pnpm
```powershell
npm install -g pnpm
```

### 5. Python 依赖（PDF 解析兜底）
```powershell
pip install pdfplumber
```
> **坑点 2**：不装会报 `PDF 解析失败（Rust 和 Python 均失败）`，因为 Rust 侧 pdfplumber 无法解析所有畸形 PDF 时，会调用 Python 脚本 `scripts/pdf_extract.py` 兜底，而它依赖 `pdfplumber` 库。

---

## 三、启动步骤（严格按顺序）

### 第 1 步：启动 Docker 基础设施

```powershell
cd 项目根目录\backend-java\src\main\resources
docker compose up -d
```

首次会下载 MySQL、Redis、Milvus、MinIO、etcd 等镜像（约 3GB），国内走镜像加速约 10-20 分钟。已下载过则秒级启动。

查看状态：

```powershell
docker compose ps
```

**坑点 1（ghcr.io 拉取失败）**：`doc-converter`（DOCX→PDF 转换）镜像在 `ghcr.io`，国内无法访问，会导致 `docker compose up -d` 整体报错中断。
- **如果只审 PDF**：把 `docker-compose.yml` 里 `doc-converter` 服务整段注释掉即可，不影响核心功能。
- **必须支持 DOCX**：在能访问 ghcr.io 的机器上执行 `docker pull` + `docker save` 导出 `.tar`，拷贝过来 `docker load`。同时本机需安装 LibreOffice（见坑点 5）。

**坑点 3（端口冲突）**：`attu`（Milvus Web 界面）占用 3000 端口。如果不需要可视化 Milvus，可注释掉该服务，避免与前端页面访问混淆。

### 第 2 步：配置 .env

在**项目根目录**确认 `.env` 文件存在。压缩包已自带 `.env`，但 `DASHSCOPE_API_KEY` 是**空的**，必须填入新电脑的密钥。关键项：

```env
DASHSCOPE_API_KEY=sk-你的阿里云DashScope密钥    # 必填！
AIBID_LLM_PROTOCOL=dashscope
AIBID_SEARCH_BACKEND=dashscope
EMBED_ENGINE=remote                            # 重要：见坑点 4
AIBID_AGENT=1
AIBID_COORDINATOR=1
```

**坑点 4（BGE-M3 模型下载失败）**：`EMBED_ENGINE=local` 会从 Hugging Face 下载 568MB 的 ONNX 模型，国内被墙导致启动即报 `无法加载 BGE-M3 模型`。改为 `remote` 后走 DashScope API 嵌入，无需下载任何模型。

### 第 3 步：初始化数据库（每次重建容器后必做）

MySQL 容器只创建空库，**不会自动建表**（项目 Flyway 已禁用）。表结构在 SQL 文件里，手动导入：

```powershell
Get-Content "项目根目录\backend-java\src\main\resources\sql\smart_tender.sql" | docker exec -i smart-mysql mysql -uroot -p1234 smart_tender_system
Get-Content "项目根目录\backend-java\src\main\resources\sql\test_data.sql" | docker exec -i smart-mysql mysql -uroot -p1234 smart_tender_system
```

> - 建议把两个 `.sql` 复制到固定文件夹（如 `桌面\mysql\`），以后直接从这里导入，不用进项目目录。
> - `test_data.sql` 含初始账号：`admin` / `123456`，`auditor` / `123456`，`manager` / `123456`。
> - 只有执行过 `docker compose down` 或换电脑/清卷才需要重新导入；重启电脑不影响。

### 第 4 步：启动 Rust AI 引擎（端口 3001）✅必须（编译）

```powershell
cd 项目根目录\backend-rust
$env:AIBID_DATA_DIR=".."
cargo run --bin server
```

> `target/` 已被压缩包排除，首次 `cargo run` 会**重新编译全部依赖**（5-10 分钟），之后秒级启动。

看到如下输出即成功（远程嵌入模式）：

```
嵌入引擎: 远程 DashScope (text-embedding-v4)
监听地址: http://127.0.0.1:3001
```

> 首次 `cargo run` 编译全部依赖，约 5-10 分钟，属正常。健康检查：浏览器打开 `http://127.0.0.1:3001/health` 返回 `{"status":"ok"}`。

### 第 5 步：启动 Java 业务网关（端口 8086）✅必须（编译）

```powershell
cd 项目根目录\backend-java
mvn clean package -DskipTests
java -jar target/smart_tender-0.0.1-SNAPSHOT.jar
```

> `target/` 已被压缩包排除，jar 需先由 `mvn package` 生成（首次下载 Maven 依赖较多，约 5-15 分钟）。

看到 `Tomcat started on port 8086` 即成功。

> **坑点 6（端口是 8086 不是 3000）**：`application.yml` 启用 `prod` profile，`application-prod.yml` 覆盖端口为 **8086**，且前端代理也指向 8086。README 写的 3000 是旧值，以 8086 为准。
> 验证：`http://127.0.0.1:8086/actuator/health` 返回 `"status":"UP"`。

### 第 6 步：启动前端（端口 5173）✅必须（安装依赖）

```powershell
cd 项目根目录\frontend
pnpm install
pnpm dev
```

> `node_modules/` 已被压缩包排除，`pnpm install` 必须执行（约 5-10 分钟，首次下载 558 个包）。

浏览器打开 `http://127.0.0.1:5173`，用 `admin / 123456` 登录。

---

## 四、全量启动顺序总结

```
1. docker compose up -d          → MySQL + Redis + Milvus
2. 导入 SQL（见第 3 步）
3. Rust 引擎 :3001               → cargo run --bin server（首次编译 5-10 分钟）
4. Java 网关 :8086               → mvn clean package 再 java -jar ...（首次编译 5-15 分钟）
5. React 前端 :5173              → pnpm install 再 pnpm dev（首次安装 5-10 分钟）
```

> 压缩包已排除三个编译/安装产物目录，因此第 3、4、5 步**首次执行必须包含编译或安装命令**，不能直接运行。

> **PowerShell 注意**：不支持 `&&` 连接命令，用 `;` 代替或分两行执行（README 里的 `&&` 是给 Git Bash 用的）。

---

## 五、常见问题排查

| 症状 | 原因 | 解决 |
|---|---|---|
| `ghcr.io ... EOF` 或卡住 | ghcr.io 被墙 | 注释 doc-converter 或 docker save/load |
| `无法加载 BGE-M3 模型` | Hugging Face 被墙 | `.env` 改 `EMBED_ENGINE=remote` |
| `PDF 解析失败（Rust 和 Python 均失败）` | 缺 pdfplumber | `pip install pdfplumber` |
| `DOCX 转 PDF 失败：找不到 LibreOffice` | 本机无 LibreOffice | 安装 LibreOffice 并设置 `LIBREOFFICE_PATH`；或改用 PDF 审核 |
| `Table 'sys_user' doesn't exist` | 表未导入 | 执行第 3 步 SQL 导入 |
| `docker compose up -d` 报 500 / API 版本错误 | Docker 守护进程卡死 | 重启 Docker Desktop 后再试 |
| 前端 `insertBefore` 报错 | 项目前端渲染 bug（审核已成功） | 刷新页面，数据已入库 |
| `doc-converter` 下载超时 | 国内访问 ghcr.io 慢 | 注释该服务，后续需要时再补 |

**LibreOffice（可选，DOCX 审核必需）**：从 <https://www.libreoffice.org/download/> 下载安装，装完设置环境变量：

```powershell
[Environment]::SetEnvironmentVariable("LIBREOFFICE_PATH", "C:\Program Files\LibreOffice\program\soffice.exe", "User")
```

---

## 六、验证清单（全绿即部署成功）

- [ ] `docker compose ps` 显示 mysql / redis / milvus 均 Up
- [ ] SQL 导入无报错，`sys_user` 表有 3 个用户
- [ ] Rust `:3001/health` 返回 `ok`
- [ ] Java `:8086/actuator/health` 返回 `UP`
- [ ] 前端 `:5173` 能登录（admin/123456）
- [ ] 上传 PDF 触发审核能跑完出结果（用 `backend-rust\tests\file\` 下的测试文件）
