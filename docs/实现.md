# AI 招标文件合规审查系统 — 实现

## 3. 文档解析与结构化管线

将原始投标文件（`.docx`）转化为下游 Agent 可直接消费的结构化数据。整条管线分三步，全部采用**确定性规则引擎**，无 LLM 参与。

```
.docx / .doc 文件
    │
    ▼
┌─ Step 1: DOCX → PDF ──────────────────────────────────────────────┐
│  src/services/docx_convert_service.rs                              │
│  LibreOffice headless 模式转换                                      │
└───────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─ Step 2: PDF → RawDocument ───────────────────────────────────────┐
│  src/services/pdf_extract_service.rs (Rust 主引擎)                  │
│  scripts/pdf_extract.py (Python 兜底引擎)                           │
│  src/domain/raw_document.rs (数据模型)                               │
└───────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─ Step 3: RawDocument → Section 树 ────────────────────────────────┐
│  src/services/sectionize_service.rs                                 │
│  7 种中文标题模式 → 嵌套章节树                                       │
└───────────────────────────────────────────────────────────────────┘
    │
    ▼
  下游消费: Chunk 切分（§4）→ Embedding → Agent 审查
```

### 3.1 Step 1: DOCX → PDF 转换

实现文件：[`src/services/docx_convert_service.rs`](../src/services/docx_convert_service.rs)

#### 技术选型

选择 **LibreOffice headless** 而非 python-docx 直读，原因：下游管线依赖 PDF 页面坐标体系（BBox），`python-docx` 能取文本但无法还原排版位置，而高亮回溯需求要求每个词都有精确的页面坐标。

| 方案 | 结论 |
|------|------|
| LibreOffice headless | ✅ 采用 — 免费、保真度高、命令行可调用 |
| Microsoft Word COM | ❌ 仅 Windows、需 Office 授权 |
| python-docx 直读 | ❌ 丢失页面坐标，与 PDF 提取管线不兼容 |
| 在线转换 API | ❌ 隐私风险、网络依赖 |

#### 核心函数

```rust
/// 将 DOCX 文件转换为 PDF。
/// 调用 LibreOffice headless 模式执行转换。
/// 生成的 PDF 文件名与输入相同，仅扩展名改为 .pdf。
pub fn convert_docx_to_pdf(input_path: &str, output_dir: &str) -> Result<PathBuf> {
    // 1. 验证输入: 文件存在 + 扩展名为 .docx/.doc
    // 2. 查找 soffice: 环境变量 LIBREOFFICE_PATH → Windows 默认路径 → PATH
    // 3. 执行转换: soffice --headless --convert-to pdf --outdir <dir> <input>
    // 4. 验证输出: 检查生成的 .pdf 是否存在且非空
}
```

#### LibreOffice 查找策略

```
优先级 1: 环境变量 LIBREOFFICE_PATH          ← 自定义安装路径
优先级 2: Windows 默认路径:
           · C:\Program Files\LibreOffice\program\soffice.exe
           · C:\Program Files (x86)\LibreOffice\program\soffice.exe
优先级 3: PATH 中的 soffice                   ← Linux/macOS 标准安装
```

```rust
fn find_soffice() -> Result<PathBuf> {
    // 1. 检查环境变量 LIBREOFFICE_PATH
    // 2. Windows 默认安装路径（#[cfg(windows)]）
    // 3. PATH 查找 (which/where soffice)
}
```

#### 命令行调用

```bash
soffice --headless --convert-to pdf --outdir <output_dir> <input.docx>
```

转换完成后验证输出文件存在于预期路径且大小 > 0。

#### 错误处理

| 错误场景 | 检测方式 | 错误信息 |
|----------|---------|---------|
| LibreOffice 未安装 | `find_soffice()` 返回 `Err` | "请安装 LibreOffice 或设置 LIBREOFFICE_PATH 环境变量" |
| 输入文件不存在 | `Path::exists()` | "输入文件不存在: {path}" |
| 不支持的格式 | 扩展名检查 | "不支持的文件格式: .{ext}，仅支持 .docx / .doc" |
| 转换过程失败 | `Command::output()` 非零退出 | 打印 stdout + stderr 供诊断 |
| 输出 PDF 未生成 | 文件系统检查 | "LibreOffice 未生成 PDF 文件" |

---

### 3.2 Step 2: PDF → RawDocument 提取

这是整条管线中最复杂、容错要求最高的一环。

#### 实现文件

| 文件 | 角色 |
|------|------|
| [`src/services/pdf_extract_service.rs`](../src/services/pdf_extract_service.rs) | Rust 主引擎 + Python 兜底调用 |
| [`scripts/pdf_extract.py`](../scripts/pdf_extract.py) | Python pdfplumber 兜底引擎 |
| [`src/domain/raw_document.rs`](../src/domain/raw_document.rs) | RawDocument / RawPage / RawWord / RawBlock / RawTable / BBox 数据模型 |

#### 双引擎策略

```
                ┌──────────────────────┐
                │      输入 PDF         │
                └──────────┬───────────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ 引擎1: Rust pdfplumber│
                │ (lopdf 后端)          │
                │ · 纯 Rust，零外部依赖  │
                │ · 速度快 (~1-5s)      │
                └──────────┬───────────┘
                           │
                      ┌────▼────┐
                      │  成功？  │
                      └────┬────┘
                      成功 │   │ 失败（解析异常/空输出）
                           │   ▼
                           │ ┌──────────────────────────┐
                           │ │ 引擎2: Python pdfplumber  │
                           │ │ (pdfminer.six 后端)       │
                           │ │ · 子进程调用              │
                           │ │ · 畸形 PDF 容错性高      │
                           │ └──────────┬───────────────┘
                           │            │
                           └──────┬─────┘
                                  ▼
                      ┌──────────────────────┐
                      │    RawDocument JSON   │
                      │  (output/raw_json/)   │
                      └──────────────────────┘
```

**为什么需要 Python 兜底？** `lopdf` 对标准 PDF 效果好，但政府标书中常见旧版 Word/WPS 导出的畸形 PDF（非标准编码、扫描件、字体损坏），`pdfminer.six` 十多年打磨的容错处理远超 `lopdf`。

#### 数据模型

```rust
// src/domain/raw_document.rs

pub struct RawDocument {
    pub document_id: String,       // UUID v4
    pub source_path: String,       // 源 PDF 路径
    pub pages: Vec<RawPage>,
}

pub struct RawPage {
    pub page_index: usize,         // 页码，0-based
    pub width: f64,                // 页面宽度（pt）
    pub height: f64,               // 页面高度（pt）
    pub text: String,              // 页面纯文本（已清洗）
    pub words: Vec<RawWord>,       // 单词列表（带 ID + BBox）
    pub blocks: Vec<RawBlock>,     // 段落块（带 ID + BBox）
    pub tables: Vec<RawTable>,     // 表格（带 ID + BBox）
    pub lines: Vec<RawLine>,       // 线段
    pub rects: Vec<RawRect>,       // 矩形区域
}
```

**ID 命名规范**：

| 层级 | 前缀 | 格式 | 示例 |
|------|------|------|------|
| Word | `w_` | `w_{页码}_{序号}` | `w_3_15` |
| Block | `b_` | `b_{页码}_{序号}` | `b_5_2` |
| Table | `t_` | `t_{页码}_{序号}` | `t_2_0` |

**BBox 坐标系**：原点在页面左上角，X 向右，Y 向下（与 PDF 坐标系一致）。

```rust
pub struct BBox {
    pub x0: f64,      // 左边界
    pub top: f64,     // 上边界（距页顶）
    pub x1: f64,      // 右边界
    pub bottom: f64,  // 下边界
}
```

#### Rust 主引擎提取流程

`pub fn extract_pdf_to_raw_json(path: &str) -> Result<RawDocument>` 对每页执行以下步骤：

**提取文本**

```rust
let raw_text = page.extract_text(&TextOptions {
    layout: true,           // 保留 PDF 坐标信息的 layout 模式
    ..Default::default()
});
```

**提取单词（带 ID 和 BBox）**

```rust
let words: Vec<RawWord> = page
    .extract_words(&WordOptions::default())
    .into_iter()
    .enumerate()
    .map(|(i, w)| RawWord {
        id: format!("w_{}_{}", page_index, i),
        text: w.text,
        bbox: BBox {
            x0: w.bbox.x0, top: w.bbox.top,
            x1: w.bbox.x1, bottom: w.bbox.bottom,
        },
    })
    .collect();
```

**文本清洗 + 重建兜底**

```rust
let cleaned = clean_layout_text(&raw_text);
let text = if cleaned.len() < raw_text.len() * 20 / 100 {
    // 高空白占比（> 80%）→ 触发词坐标重建
    reconstruct_text_from_words(&words)
} else {
    cleaned
};
```

**段落块聚合**

```rust
let blocks = compute_blocks(&words, page_index);
```

**表格、线条、矩形提取**

```rust
let tables: Vec<RawTable> = page.extract_tables(&TableSettings::default())
    .into_iter().enumerate()
    .map(|(i, rows)| RawTable {
        id: format!("t_{}_{}", page_index, i),
        bbox: None,  // Rust 版 lopdf 不返回表格坐标
        rows,
    }).collect();
```

#### 文本清洗：两大正则规则

中文标书 PDF 常用绝对定位渲染每个字符，`layout=True` 产生大量填充空格。清洗管线使用与 Python 引擎完全相同的逻辑：

```
原始: "投  标  人  须  在  东  莞  地  区  设  有  常  驻  服  务  机  构"
清洗后: "投标人须在东莞地区设有常驻服务机构"
```

```rust
// 规则1: 合并被空格拆散的中文词组（[一-鿿])\s+([一-鿿]) → $1$2）
// 迭代最多 5 轮
static CJK_SPACE_RE: LazyLock<Regex> = LazyLock::new(|| {
    Regex::new(r"([一-鿿])\s+([一-鿿])").expect("CJK regex")
});

// 规则2: 压缩连续空格（2+ 空格 → 2 空格，保留词间分隔）
static MULTI_SPACE_RE: LazyLock<Regex> = LazyLock::new(|| {
    Regex::new(r" {2,}").expect("multi-space regex")
});
```

#### 文本重建：从单词坐标建文本

当 `clean_layout_text` 后文本长度不足原始的 20% 时触发。Rust 和 Python 引擎实现相同算法：

```
reconstruct_text_from_words():
  1. 取所有 word 高度的中位数 → line_height
  2. 按 (top, x0) 排序
  3. 按 Y 坐标分组为行: word.top - 当前行 top < 1.2×line_height → 同一行
  4. 行内按 X 排序，按列间距分"列段":
     列间距 < 8× 平均字符宽度 → 同一列段
     列间距 ≥ 8× 平均字符宽度 → 新列段（表格/多列布局）
  5. 列段间用双空格连接，行间用换行连接
```

#### 段落块（Block）聚合算法

从离散单词 → 语义段落块的聚合算法，Rust 和 Python 引擎完全一致：

```
compute_blocks(words, page_index):
  Step 1 — 按 Y 坐标分组为"文本行": 垂直间距 < 1.2×line_height → 同一行
  Step 2 — 按行间距合并为"段落块": 行间距 > 1.8×line_height → 段落边界
  Step 3 — 构建 Block:
    bbox = 块内所有 word 的并集矩形
    id = "b_{page_index}_{block_index}"
    type = heading (单行 ≤ 10 词) | paragraph
    text = 各行文本拼接
  const HEADING_GAP_RATIO: f64 = 1.8;
```

```
示意:
┌────────────────────────────────────────┐
│  第三章 评审方法和标准           ← b_3_0 │  (单行 ≤ 10词 → heading)
│  3.1 评审方法采用综合评分法...   ← b_3_1 │  (多行 → paragraph)
│  总分为100分，其中技术部分占60分...     │
│  3.2 评审程序                   ← b_3_2 │  (行间距 > 1.8× → 新段落)
└────────────────────────────────────────┘
```

#### Python 兜底引擎

[`scripts/pdf_extract.py`](../scripts/pdf_extract.py) 实现了与 Rust 引擎输出格式完全一致的提取：

```
用法: python scripts/pdf_extract.py <input.pdf> <output.json>

与 Rust 引擎对齐:
  ✓ 相同的 ID 命名规则 (w_{p}_{i} / b_{p}_{i} / t_{p}_{i})
  ✓ 相同的 BBox 结构 {x0, top, x1, bottom}
  ✓ 相同的文本清洗策略 (CJK_SPACE_RE + MULTI_SPACE_RE)
  ✓ 相同的 Block 聚合算法 (1.2× line_height + 1.8× heading_gap)
  ✓ 相同的文本重建兜底 (< 20% 触发 reconstruct_text_from_words)
  ✓ Python 版额外优势: find_tables() 可获取表格精确 BBox
```

Rust 侧通过子进程调用：

```rust
pub fn extract_with_python(input_path: &str, output_path: &str) -> Result<()> {
    let output = Command::new("python")
        .args(["scripts/pdf_extract.py", input_path, output_path])
        .output()?;
    if !output.status.success() {
        anyhow::bail!("Python 脚本执行失败: {}", stderr);
    }
    // 验证输出 JSON 文件存在且非空
}
```

#### 输出位置

```
output/raw_json/{文档名}_raw.json
```

---

### 3.3 Step 3: RawDocument → Section 树构建

实现文件：[`src/services/sectionize_service.rs`](../src/services/sectionize_service.rs)

#### 技术选型：为什么用正则而不用 LLM

| 维度 | 正则规则引擎 (当前方案) | LLM 方案 |
|------|----------------------|----------|
| 标题模式 | 中文标书标题高度规整，正则可 100% 覆盖 | 适合非结构化，标书本就结构化 |
| 层级判定 | 模式→层级映射确定性的 | 需理解上下文，可能不一致 |
| 速度 | 615 section < 10ms | 数十秒 |
| 成本 | 零 | 数千 tokens/份 |

#### 七种标题模式 → 五级层级

按优先级排列，靠前的先匹配（一行只匹配一个模式）：

```rust
static HEADING_PATTERNS: LazyLock<Vec<HeadingPattern>> = LazyLock::new(|| {
    vec![
        // Level 1: 第X章
        HeadingPattern { pattern_type: "chapter", level: 1,
            regex: Regex::new(r"^第[一二三四五六七八九十百千]+章") },
        // Level 2: 第X节
        HeadingPattern { pattern_type: "section", level: 2,
            regex: Regex::new(r"^第[一二三四五六七八九十百千]+节") },
        // Level 2: 一、二、三、...
        HeadingPattern { pattern_type: "cjk_numbered", level: 2,
            regex: Regex::new(r"^[一二三四五六七八九十百千]+[、.．]\s*\S") },
        // Level 3: （一）（二）...
        HeadingPattern { pattern_type: "paren_cjk", level: 3,
            regex: Regex::new(r"^[（(][一二三四五六七八九十百千]+[）)]\s*\S") },
        // Level 4: 1. 2、3) ...
        HeadingPattern { pattern_type: "digit_dot", level: 4,
            regex: Regex::new(r"^\d+[.、)）]\s*\S") },
        // Level 5: （1）（2）(1) ...
        HeadingPattern { pattern_type: "paren_digit", level: 5,
            regex: Regex::new(r"^[（(]\d+[）)]\s*\S") },
        // Level 4: 第X条 (合同条款)
        HeadingPattern { pattern_type: "article", level: 4,
            regex: Regex::new(r"^第[一二三四五六七八九十百千]+条") },
    ]
});
```

**优先级设计逻辑**：`第X章` 必须排第一——`一、` 和 `1.` 既可作章标题也可作节标题，`chapter` 和 `section` 是最明确的层级信号，必须优先消费。

#### 三重过滤规则

**过滤 1 — 纯数字行（页码）**

```rust
fn is_page_number(line: &str) -> bool {
    let trimmed = line.trim();
    if trimmed.len() > 3 { return false; }
    trimmed.chars().all(|c| c.is_ascii_digit() || c == ' ')
}
// "1" → true, "92" → true, "第一章" → false
```

**过滤 2 — 数字序号匹配行过长**

```rust
// level >= 4（digit_dot）且标题 > 80 字符 → 大概率是条款正文中的编号，跳过
if pattern.level >= 4 && title.chars().count() > 80 {
    continue;
}
```

**过滤 3 — 同一行匹配多个模式** → 取层级最高的（level 数值最小）。

#### 强调符号预处理

去除行首的 ★▲●■◆◎☆ 等 18 种常见符号后再匹配模式：

```rust
const EMPHASIS: &[char] = &[
    '★', '▲', '●', '■', '◆', '◎', '☆',
    '△', '▽', '◁', '▷', '◇', '□', '○',
    '✔', '☑', '❗', '✓',
];

fn strip_emphasis_prefix(s: &str) -> &str { ... }
```

匹配策略：原始行匹配 → 命中取匹配位置到行尾；去符号版匹配 → 命中取原始行整体（含 ★ 标记）。

#### 标题截断检测与修复（PDF 折行）

PDF 物理折行可能把标题切成两半：

```rust
fn is_title_truncated(title: &str, body_text: &str) -> bool {
    // 条件1: 标题不以 。！？）): 结尾（非句子结束标点）
    //        注：逗号（，,）不是结束标点 → 以逗号结尾 = 截断信号
    // 条件2: title 长度 ≥ 15 字符
    // 条件3: body_text 首字符是 CJK 汉字或小写字母（续接特征）
    // 条件4: 仅对 level >= 3 检测（章/节标题本身即完整）
}

fn merge_truncated_title(title: &str, body_text: &str) -> (String, String) {
    // 逐行从 body_text 续接内容到 title
    // 遇到 。！？ 停止 → 句子结束
    // 遇到另一个标题模式 → 停止
    // body_text 耗尽 → 停止
}
```

#### 树构建算法：两阶段栈式组装

```
Phase 1 — 扫描构建扁平列表 + 记录父子关系
  遍历 HeadingCandidate[]:
    维护"祖先栈" stack
    对于当前候选:
      ① 弹出栈中所有 level >= 当前 level 的节点
      ② 父节点 = 栈顶（最近的 level 更低的节点）
      ③ 当前节点入栈 → 记录 parent_of[i]

Phase 2 — 收集子节点索引
  children_of[i] = {所有 parent_of[j] == i 的 j}

Phase 3 — 递归组装树
  从根节点 (parent_of[i] == None) 出发:
    递归 take_section_from_flat():
      取出 sections[idx]，遍历 children_of[idx]，组装 Section.children
```

**栈变化示意**：

```
候选: [第一章(L1), 一、概述(L2), （一）范围(L3), 二、要求(L2)]

第一章:    push → [第一章]                    parent=无
一、概述:  push → [第一章, 一、概述]           parent=第一章
（一）:    push → [第一章, 一、概述, （一）]    parent=一、概述
二、要求:  L2 → pop(L3), pop(L2)
           push → [第一章, 二、要求]           parent=第一章

树: 第一章
      ├── 一、概述
      │     └── （一）范围
      └── 二、要求
```

完整实现参见 [`build_section_tree()`](../src/services/sectionize_service.rs)。

#### 正文提取策略

```rust
fn extract_section_body(candidate, next_boundary, all_blocks, block_ids) -> String {
    // 1. 找到标题行 → found_title = true
    // 2. 从标题行之后收集文本行
    // 3. 遇到以下停止:
    //    a. 任意子标题模式 → 子 section 正文开始
    //    b. next_boundary 标题 → 同级 section 开始
    //    c. block_ids 耗尽
    // 4. 过滤空行和页码
    // 5. 返回拼接（中文 PDF 块内换行均为物理折行伪影，不保留 \n）
}
```

#### 输出数据结构

```rust
pub struct Section {
    pub level: u8,                // 1=章, 2=节, 3=小节, 4=条目, 5=子条目
    pub title: String,            // 标题文本（已清洗/修复截断）
    pub pattern: String,          // 匹配的模式类型
    pub page_start: usize,        // 起始页码 (0-based)
    pub page_end: usize,          // 结束页码 (0-based)
    pub block_ids: Vec<String>,   // 本节包含的所有 block ID（回溯高亮）
    pub body_text: String,        // 正文（标题之下、子标题之上）
    pub children: Vec<Section>,   // 子章节（递归嵌套）
}

pub struct SectionizeOutput {
    pub document_id: String,
    pub source_path: String,
    pub sections: Vec<Section>,
    pub stats: SectionizeStats,   // total_sections, level_counts, orphan_blocks
}
```

**输出位置**：`output/sections/{文档名}_sections.json`

#### 孤儿 Block 处理

不归属于任何 Section 的 block（如封面、目录页）：
- 保留在 RawDocument 中供全文搜索使用
- 在 Agent 的 `search_document` 工具中可被命中
- `stats.orphan_blocks` 作为数据质量指标——异常高（> 30%）说明标号体系未被覆盖

---

### 3.4 关键文件与实现状态

| 文件 | 角色 | 状态 |
|------|------|------|
| `src/services/docx_convert_service.rs` | DOCX → PDF（LibreOffice headless） | ✅ |
| `src/services/pdf_extract_service.rs` | PDF → RawDocument（双引擎 + 文本清洗） | ✅ |
| `scripts/pdf_extract.py` | PDF → RawDocument（Python 兜底） | ✅ |
| `src/domain/raw_document.rs` | RawDocument 数据模型 + ID 规范 | ✅ |
| `src/services/sectionize_service.rs` | RawDocument → Section 树 | ✅ |

---

## 4. 条款级 Chunk 切分策略

从 Section 树到条款级 Chunk，核心原则：**一个 chunk = 一个可以独立理解、独立评估的完整语义单元**。不是简单地把每个叶子节点当成一个 chunk。

### 4.1 实现文件

| 文件 | 角色 | 状态 |
|------|------|------|
| `src/domain/chunk.rs` | Chunk / ChunkType / ChunkingConfig 数据模型 | 🔲 待实现 |
| `src/services/chunking_service.rs` | 五规则切分引擎 + embed_text | 🔲 待实现 |

### 4.2 数据模型

```rust
// src/domain/chunk.rs

/// 条款级语义切块，是 Agent 审查的最小单位。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Chunk {
    pub chunk_id: String,              // "ch_042"
    pub chunk_type: ChunkType,         // Leaf | Merged | Split
    pub section_path: Vec<String>,     // 从根章节到当前节点的标题链
    pub text: String,                  // chunk 完整文本（含标题行）
    pub page_start: usize,             // 起始页码 (0-based)
    pub page_end: usize,               // 结束页码 (0-based)
    pub source_block_ids: Vec<String>, // 来源 block ID（回溯高亮）
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(tag = "type")]
pub enum ChunkType {
    Leaf,                                       // 规则1
    Merged { rule: String, child_count: usize }, // 规则2+3
    Split { part: usize, total: usize },         // 规则4
}

pub struct ChunkingConfig {
    pub merge_min_len: usize,       // 规则3: 短叶子合并阈值，default: 100
    pub split_max_len: usize,       // 规则4: 过长硬切阈值，default: 1500
    pub split_overlap: usize,       // 规则4: 硬切 overlap，default: 200
    pub embed_ctx_depth: usize,     // 规则5: 嵌入层级前缀深度，default: 2
}

impl Default for ChunkingConfig {
    fn default() -> Self {
        Self { merge_min_len: 100, split_max_len: 1500,
               split_overlap: 200, embed_ctx_depth: 2 }
    }
}
```

### 4.3 主入口

```rust
// src/services/chunking_service.rs

/// 从 Section 树切分为 Chunk 列表。
pub fn chunk_sections(sections: &[Section], config: &ChunkingConfig) -> Vec<Chunk> {
    let mut chunks = Vec::new();
    traverse_and_chunk(sections, &vec![], config, &mut chunks);
    // 按页面顺序排序并分配 chunk_id
    for (i, chunk) in chunks.iter_mut().enumerate() {
        chunk.chunk_id = format!("ch_{:03}", i);
    }
    chunks
}
```

### 4.4 规则 1 — 自包含叶子节点直接成 chunk

有 `body_text` 且无子节点的 section 是完整语义单元，直接成为 Leaf 型 chunk：

```rust
fn try_chunk_leaf(
    section: &Section, parent_path: &Vec<String>,
    config: &ChunkingConfig, chunks: &mut Vec<Chunk>,
) -> bool {
    // 条件: body_text 非空 && children 为空
    if section.body_text.is_empty() || !section.children.is_empty() {
        return false;
    }
    let mut path = parent_path.clone();
    path.push(section.title.clone());
    let text = format!("{}\n{}", section.title, section.body_text);

    // 过长 → 规则4硬切
    if text.chars().count() > config.split_max_len {
        split_long_chunk(path, text, section, config, chunks);
        return true;
    }

    chunks.push(Chunk {
        chunk_id: String::new(),
        chunk_type: ChunkType::Leaf,
        section_path: path, text,
        page_start: section.page_start, page_end: section.page_end,
        source_block_ids: section.block_ids.clone(),
    });
    true
}
```

```
示例:
L2: 三.获取磋商文件   body="时间：详见磋商公告...地点：..."
→ chunk_042:
    chunk_type: Leaf
    section_path: ["第一章 磋商邀请", "三.获取磋商文件"]
    text: "三.获取磋商文件\n时间：详见磋商公告..."
```

### 4.5 规则 2 — 纯容器节点不生成 chunk，子节点向上聚合

`body_text` 为空、仅有子节点的 section 是"容器"，不单独成 chunk，递归处理子节点时带上容器标题作为上下文：

```rust
fn process_container(
    section: &Section, parent_path: &Vec<String>,
    config: &ChunkingConfig, chunks: &mut Vec<Chunk>,
) {
    let mut new_path = parent_path.clone();
    new_path.push(section.title.clone());
    for child in &section.children {
        traverse_and_chunk(child, &new_path, config, chunks);
    }
}
```

```
聚合效果:
L2: 二.供应商的资格要求          ← 容器 (body_text 为空)
  L4: 1.供应商应具备《政府采购法》第二十二条...  ← 容器
    L5: 1）具有独立承担民事责任的能力...     ← 叶子
    L5: 2）有依法缴纳税收...                 ← 叶子
    ...

→ 5 个 L5 叶子合并为 1 个 chunk (~280 字):
    chunk_type: Merged { rule: "container_aggregation", child_count: 5 }
    section_path: ["第一章 磋商邀请", "二.供应商的资格要求",
                    "1.供应商应具备《政府采购法》第二十二条规定的条件"]
```

Agent 审查资格条件时，一个 chunk 包含法条全部条件，无需跨 chunk 拼凑。

### 4.6 规则 3 — 相邻独立叶子合并，直到触及上限

同一父节点下，多个独立叶子如果各自太短（< `merge_min_len` = 100 字），合并为一个语义完整的 chunk：

```rust
fn merge_adjacent_leaves(
    leaves: &[&Section], parent_path: &Vec<String>,
    config: &ChunkingConfig, chunks: &mut Vec<Chunk>,
) {
    let mut merge_buffer: Vec<&Section> = Vec::new();
    let mut merge_len: usize = 0;

    for leaf in leaves {
        let leaf_text = format!("{}\n{}", leaf.title, leaf.body_text);
        let leaf_len = leaf_text.chars().count();

        if leaf_len < config.merge_min_len {
            merge_buffer.push(leaf);
            merge_len += leaf_len;
            // 超过上限 → 先消化当前缓冲区
            if merge_len > config.split_max_len {
                flush_merge_buffer(&merge_buffer, parent_path, config, chunks);
                merge_buffer.clear(); merge_len = 0;
            }
        } else {
            // 够长 → 先消化缓冲区，再单独成 chunk
            flush_merge_buffer(&merge_buffer, parent_path, config, chunks);
            merge_buffer.clear(); merge_len = 0;
            try_chunk_leaf(leaf, parent_path, config, chunks);
        }
    }
    flush_merge_buffer(&merge_buffer, parent_path, config, chunks);
}
```

```
示例:
L4: 1.项目地点：东莞理工学院松山湖校区6号教学楼。  ← 41 字
L4: 2.项目工期：合同签订后 60 个日历日内完成。      ← 35 字
L4: 3.质保期：项目验收合格之日起 2 年。             ← 30 字
L4: 4.完工后，成交供应商需提供竣工图纸。            ← 47 字

→ 合并为 1 个 chunk (~150 字):
    chunk_type: Merged { rule: "adjacent_merge", child_count: 4 }
```

### 4.7 规则 4 — 过长 chunk 在段落边界硬切，保留 overlap

单个 chunk > 1500 字 → 找段落边界切开，前后保留 200 字 overlap：

```rust
fn split_long_chunk(
    path: Vec<String>, text: String,
    section: &Section, config: &ChunkingConfig, chunks: &mut Vec<Chunk>,
) {
    let total = text.chars().count();
    let boundaries = find_para_boundaries(&text);  // \n\n 或 \n(?=[一二三四五...

    let mut parts: Vec<String> = Vec::new();
    let mut pos = 0;
    while pos < total {
        let mut end = (pos + config.split_max_len).min(total);
        if end >= total {
            parts.push(text[pos..].to_string());
            break;
        }
        // 回退到最近的段落边界
        if let Some(&b) = boundaries.iter().rev().find(|&&b| b >= pos && b <= end) {
            end = b;
        }
        parts.push(text[pos..end].to_string());
        pos = end.saturating_sub(config.split_overlap);
    }

    for (i, part) in parts.iter().enumerate() {
        chunks.push(Chunk {
            chunk_id: String::new(),
            chunk_type: ChunkType::Split { part: i + 1, total: parts.len() },
            section_path: path.clone(),
            text: part.clone(),
            page_start: section.page_start, page_end: section.page_end,
            source_block_ids: section.block_ids.clone(),
        });
    }
}

fn find_para_boundaries(text: &str) -> Vec<usize> {
    // 段落边界: \n\n 或 \n 后跟中文序号（一～十）
}
```

### 4.8 规则 5 — 嵌入文本携带层级上下文

向量嵌入时携带层级前缀，避免不同章节的编号条目在向量空间中混淆：

```rust
impl Chunk {
    /// 生成带层级上下文的嵌入文本。
    /// 裸文本: "1）具有独立承担民事责任的能力..."
    /// 嵌入文本: "【供应商的资格要求 > 政府采购法第二十二条】
    ///            1）具有独立承担民事责任的能力..."
    pub fn embed_text(&self, ctx_depth: usize) -> String {
        let ctx = self.section_path.iter().rev().take(ctx_depth).rev()
            .cloned().collect::<Vec<_>>().join(" > ");
        if ctx.is_empty() { self.text.clone() }
        else { format!("【{}】\n{}", ctx, self.text) }
    }
}
```

Agent 用 `search_document("资格条件 独立承担")` 搜索时，能精确命中这条，而不会误匹配到其他章节的 `"1）..."` 编号条目。

### 4.9 切分决策总览

| 条件 | 规则 | 动作 |
|------|------|------|
| `body_text` 非空 + 无子节点 + 长度 ≤ 1500 | 规则1 | → `Leaf` chunk |
| `body_text` 非空 + 无子节点 + 长度 > 1500 | 规则1 → 规则4 | → `Split` chunks (带 overlap) |
| `body_text` 为空 + 有子节点 | 规则2 | → 向下传递容器路径，聚合子节点 |
| 多个叶子各自 < 100 字 + 同在父节点下 | 规则3 | → `Merged` chunk (合并后 ≤ 1500) |
| 所有 chunk | 规则5 | → `embed_text()` 携带层级前缀 |

### 4.10 树遍历骨架

```rust
fn traverse_and_chunk(
    section: &Section, parent_path: &Vec<String>,
    config: &ChunkingConfig, chunks: &mut Vec<Chunk>,
) {
    // 规则1: 自包含叶子 → 直接成 chunk
    if try_chunk_leaf(section, parent_path, config, chunks) { return; }

    // 规则2: 容器节点 → 向下传递路径
    let mut new_path = parent_path.clone();
    new_path.push(section.title.clone());

    if section.children.is_empty() { return; }  // 纯标题占位，跳过

    // 分离叶子和容器子节点
    let (leaves, containers): (Vec<_>, Vec<_>) = section.children
        .iter()
        .partition(|c| c.children.is_empty() && !c.body_text.is_empty());

    // 规则3: 相邻独立叶子 → 合并
    merge_adjacent_leaves(
        &leaves.iter().map(|s| *s).collect::<Vec<_>>(),
        &new_path, config, chunks,
    );

    // 容器子节点 → 递归
    for child in &containers {
        traverse_and_chunk(child, &new_path, config, chunks);
    }
}
```

### 4.11 输出位置

```
output/chunks/{文档名}_chunks.json
```

### 4.12 技术选型：代码规则 vs LLM

| 维度 | 代码规则切分 | LLM 方案 |
|------|------------|----------|
| 确定性 | 同样输入永远同样输出 | 有随机性 |
| 速度 | < 10ms | 1-3s + token 消耗 |
| 覆盖度 | 规则 1-5 覆盖全部结构语义 | 看 sections.json 只能做结构判断 |
| 成本 | 零 | 每份文档 ~数千 tokens |

LLM 的角色是切分完成后的**质量审查**：抽样 5-10 个 chunk → LLM 检查语义完整性 → 有问题才触发人工介入。

---

## 阶段1 已走通的全链路

```
.docx → LibreOffice → .pdf → pdf_extract_service → RawDocument JSON
       → sectionize_service → Section 树 JSON
       → chunking_service（待实现） → Chunk[] JSON → Embedding → Agent 审查
```
