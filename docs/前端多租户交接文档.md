# 前端多租户 MVP 交接文档

基线：`luomo-dev` 的 `d6f2336`（2026-08-07）。本文给前端联调和页面改造使用，按“已合入”和“待验收”分开描述。

参考契约：

- [多租户接口契约](../backend-java/docs/多租户接口契约.md)
- [多租户接口 OpenAPI JSON](../backend-java/docs/tenant-openapi.json)
- [租户领域模型 ADR](adr/tenant-model.md)
- [租户隔离 ADR](adr/tenant-isolation.md)

## 1. 实现状态

### 已 Ready

| 模块 | 状态 | 前端现在可以依赖的内容 |
|---|---|---|
| M2 会话认证 | 已合入 | 登录、刷新、退出、当前会话快照和当前基线下的切租户接口 |

M2 的实现提交是 `ba910c4`，已经包含在本文基线中。它不等于租户列表、成员管理或业务资源已经完成租户隔离。

### Pending

| 模块 | 状态 | 尚未作为上线能力承诺的内容 |
|---|---|---|
| M3 租户 MVP API | 主线程验收中 | `GET/POST /api/tenants`、`GET /api/tenants/current`、成员列表，以及租户创建后的联调闭环 |
| M4 资源租户隔离 | 主线程验收中 | KnowledgeFile、Project、Tender、AuditTask 的查询、写入、下载、预览、结果和缓存按租户隔离 |
| M5 Java-Rust 与 SSE | 主线程验收中 | Java 到 Rust 的租户 Header/HMAC、SSE 旧会话失效、回放和跨租户负向测试 |

M3、M4、M5 都没有在本文中视为已合并；前端不要据此把 Pending 接口标记为生产可用。

## 2. 统一约定

- 外部 API 基础路径是 `/api`，请求和 JSON 响应使用 UTF-8。
- 受保护接口发送 `Authorization: Bearer <token>`。
- 浏览器不得发送 `X-Tenant-Id`。这个 Header 只属于 Java 到 Rust 的内部调用。
- 租户 API 的 `tenant_id`、`user_id`、`member_id`、`owner_user_id` 使用十进制字符串。前端内部也统一按 `string` 保存，不能用 JavaScript `number` 做主键。
- `tenant_id` 出现在路径或切租户请求体时，只是请求切换到哪个候选租户。服务端仍以 Bearer 会话、Redis Session、成员关系和租户状态解析 `CurrentTenant`。
- `role`、`permissions`、`is_current` 只用于页面展示和按钮显隐，不是前端授权依据。
- 租户域接口的错误判断使用 HTTP 状态和 `data.error_code`，不要匹配中文 `msg`。

统一 JSON 外壳：

```json
{
  "code": 200,
  "msg": "success",
  "data": {},
  "timestamp": 1786019400123
}
```

错误响应仍使用同一外壳。`request_id` 要保留下来，后端排查需要它：

```json
{
  "code": 403,
  "msg": "当前角色无权执行该操作",
  "data": {
    "error_code": "TENANT_ROLE_FORBIDDEN",
    "request_id": "8b9c0e7f-9d8b-4f86-b77d-9a3d4c2f5001",
    "details": {
      "required_permission": "tenant.members.role.write"
    }
  },
  "timestamp": 1786019400123
}
```

## 3. M2 已合入的认证接口

当前基线已有的 Java Controller 路径：

| 方法 | 路径 | 请求 | 成功响应 |
|---|---|---|---|
| POST | `/api/auth/login` | JSON：`username`、`password` | `Result<AuthSession>` |
| POST | `/api/auth/refresh` | Bearer；无请求体 | `Result<AuthSession>`；旧 token 失效 |
| POST | `/api/auth/logout` | Bearer；无请求体 | `Result<void>`；`data` 为空 |
| POST | `/api/auth/switch-tenant` | Bearer；JSON：`tenant_id` | `Result<AuthSession>`；旧 token 失效 |

登录请求：

```json
{
  "username": "alice",
  "password": "<password>"
}
```

当前基线的切租户请求：

```json
{
  "tenant_id": "20002"
}
```

切换成功后服务端递增 `session_version`，签发新 token，并返回完整会话。并发中的旧请求可能收到 `401 TENANT_SESSION_STALE`，见第 8 节。

### 3.1 AuthSession 的真实字段

`login`、`refresh`、当前基线的 `switch-tenant` 都把下列对象放在 `data` 中：

```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9...",
  "token_type": "Bearer",
  "expires_in": 86400,
  "session_version": 3,
  "user_info": {
    "user_id": "10001",
    "username": "alice",
    "real_name": "Alice"
  },
  "current_tenant": {
    "tenant_id": "20001",
    "tenant_code": "acme-bid",
    "name": "Acme 招标团队",
    "status": "ACTIVE",
    "role": "ADMIN",
    "permissions": ["tenant.read", "tender.write"],
    "is_current": true
  },
  "tenants": [
    {
      "tenant_id": "20001",
      "tenant_code": "acme-bid",
      "name": "Acme 招标团队",
      "status": "ACTIVE",
      "role": "ADMIN",
      "permissions": ["tenant.read", "tender.write"],
      "is_current": true
    }
  ]
}
```

- `token` 只放在认证状态中，调用业务接口时拼成 `Authorization: Bearer ${token}`。
- `expires_in` 是服务端给出的秒数，不要据此生成或修改 `session_version`。
- `session_version` 只保存和比较，不由前端递增。
- 没有 ACTIVE 成员时，`current_tenant` 为 `null`，`tenants` 为空；业务接口会返回 `400 TENANT_REQUIRED`。
- tenant-v1 约定 `user_info.real_name`。当前 `UserInfoVO` 的 Java 字段名是 `realName`，源码只给 `user_id` 加了 JSON 注解；联调看原始响应，适配层可暂时兼容 `real_name ?? realName`，页面统一使用 `real_name`。

### 3.2 冻结契约中的切租户路径

tenant-v1 还冻结了：

```text
POST /api/tenants/{tenantId}/switch
Authorization: Bearer <token>
```

请求体为空，响应是 `AuthSession`。目标租户不是当前用户成员时返回 `404 TENANT_NOT_FOUND`。这个路径属于 M3/M4 验收范围；主线程确认前，前端适配层使用当前基线的 `/api/auth/switch-tenant`，页面不要直接写死两套路径。

## 4. 租户和成员接口（Pending）

以下接口以 tenant-v1 和 M3 Controller/VO 为准，但 M3 仍在主线程验收中。调用前先确认部署环境已经包含对应实现。

### 4.1 租户列表、创建和当前租户

| 方法 | 路径 | 请求 | `data` 真实字段 |
|---|---|---|---|
| GET | `/api/tenants` | Bearer；当前 M3 Controller 无必填 query | `current_tenant_id`、`items` |
| POST | `/api/tenants` | Bearer；JSON：`name` 必填，`tenant_code`、`plan_code`、`settings` 可选 | 完整 `Tenant` |
| GET | `/api/tenants/current` | Bearer | `TenantSummary`；没有当前租户时 `400 TENANT_REQUIRED` |

`GET /api/tenants` 示例：

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "current_tenant_id": "20001",
    "items": [
      {
        "tenant_id": "20001",
        "tenant_code": "acme-bid",
        "name": "Acme 招标团队",
        "status": "ACTIVE",
        "role": "ADMIN",
        "permissions": ["tenant.read", "tender.write"],
        "is_current": true
      }
    ]
  },
  "timestamp": 1786019400123
}
```

`TenantSummary` 字段：`tenant_id`、`tenant_code`、`name`、`status`、`role`、`permissions`、`is_current`。

`POST /api/tenants` 请求允许的字段：

```json
{
  "name": "Acme 招标团队",
  "tenant_code": "acme-bid",
  "plan_code": "STANDARD",
  "settings": {
    "timezone": "Asia/Shanghai"
  }
}
```

前端不能提交 `owner_user_id`、`status`、`version` 或 `tenant_id`。创建成功后当前用户成为唯一 `OWNER`。`Tenant` 在摘要字段上增加 `owner_user_id`、`plan_code`、`settings`、`version`、`created_at`、`updated_at`。

注意两个待确认点：OpenAPI 给 `GET /api/tenants` 列出了 `page`、`size` 参数，但当前 M3 Controller 没有读取它们；`GET /api/tenants/current` 的正文说明提到 `session_version`，OpenAPI 的 `TenantSummary` 和当前 VO 没有该字段。前端统一从 AuthSession 读取 `session_version`，不要依赖 current 接口补充它。

### 4.2 成员列表

```text
GET /api/tenants/{tenantId}/members?page=1&size=20
Authorization: Bearer <token>
```

路径中的 `tenantId` 必须使用当前会话对应租户。返回字段是 `page`、`size`、`total`、`items`；item 固定包含 `member_id`、`tenant_id`、`user_id`、`username`、`real_name`、`role`、`permissions`、`status`、`joined_at`、`last_seen_at`。时间是 UTC RFC 3339 字符串，`last_seen_at` 可以为 `null`。

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "page": 1,
    "size": 20,
    "total": 1,
    "items": [
      {
        "member_id": "30001",
        "tenant_id": "20001",
        "user_id": "10002",
        "username": "bob",
        "real_name": "Bob",
        "role": "AUDITOR",
        "permissions": ["tenant.read", "audit.start"],
        "status": "ACTIVE",
        "joined_at": "2026-08-06T12:30:00.123Z",
        "last_seen_at": "2026-08-06T13:30:00.123Z"
      }
    ]
  },
  "timestamp": 1786019400123
}
```

## 5. 前端本地状态

登录、刷新、切租户成功后整体替换 AuthSession，不要分别更新 token 和租户字段：

```ts
type AuthState = {
  token: string | null;
  token_type: "Bearer" | null;
  expires_in: number | null;
  session_version: number | null;
  user_info: { user_id: string; username: string; real_name?: string | null } | null;
  current_tenant: TenantSummary | null;
  tenants: TenantSummary[];
};

type TenantSummary = {
  tenant_id: string;
  tenant_code: string;
  name: string;
  status: "ACTIVE" | "DISABLED" | "DELETED";
  role: "OWNER" | "ADMIN" | "AUDITOR" | "MEMBER" | "VIEWER";
  permissions: string[];
  is_current: boolean;
};
```

- token 优先放内存；若要求刷新页面后保留登录态，使用 `sessionStorage`，退出时同步清空。不要把 token 放到 URL、日志或埋点。
- `current_tenant` 是服务端快照；下拉框尚未提交的选择放在临时变量 `switchTargetId` 中。
- `tenants` 只是候选列表。切换成功后以新 AuthSession 的 `tenants` 和 `current_tenant` 覆盖旧值。
- 查询缓存带租户命名空间，例如 `["tenant", tenantId, "projects"]`。它只防缓存串租户，不能替代服务端授权。
- 切租户删除旧租户的 `Last-Event-ID`、SSE 连接、未完成请求和业务缓存；新租户连接从无游标开始。

## 6. 业务接口和真实响应字段（资源隔离 Pending）

这些路径来自当前 Java Controller。返回字段是旧业务 VO 的实际字段，保持 camelCase；它们与 tenant-v1 的 snake_case 租户字段并存。M4 验收完成前，不能把“路径存在”理解成“已经完成租户隔离”。

### 6.1 KnowledgeFile

Controller 基础路径：`/api/knowledge-files`。

| 方法 | 路径 | 请求 | 返回 |
|---|---|---|---|
| GET | `/api/knowledge-files` | query：`category`、`tags`、`applicable_scope`、`sortBy` 默认 `asc`、`page` 默认 `1`、`size` 默认 `10` | `Result<PageResult>` |
| GET | `/api/knowledge-files/search` | query：`keyword`、`sortBy`、`page`、`size` | `Result<PageResult>` |
| POST | `/api/knowledge-files/upload` | multipart：`file`、`category` 必填；`fileName`、`tags`、`applicable_scope`、`description`、`status` 可选 | 成功时 `data` 为空 |
| PUT | `/api/knowledge-files/{fileId}` | multipart：`category` 必填，其余字段同上传，`file` 可选 | 成功时 `data` 为空 |
| PATCH | `/api/knowledge-files/{fileId}/status` | JSON：`{"status": 1}` | 成功时 `data` 为空 |
| DELETE | `/api/knowledge-files/{fileId}` | 无请求体 | 成功时 `data` 为空 |
| GET | `/api/knowledge-files/{fileId}/download` | 无请求体 | 二进制；不存在 404 |
| GET | `/api/knowledge-files/{fileId}/preview` | 无请求体 | PDF 二进制；不支持类型 415 |

列表的 `data` 是旧 `PageResult`，字段只有 `total` 和 `records`，不是 tenant-v1 的 `page/size/items`。`KnowledgeFileVO` 字段：`id`、`fileName`、`fileSize`、`fileType`、`category`、`tag`、`description`、`applicableScope`、`status`、`version`、`uploadUserId`、`uploadUserName`、`uploadTime`、`updateTime`。请求参数叫 `tags`，响应字段叫 `tag`。

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "total": 1,
    "records": [
      {
        "id": 501,
        "fileName": "采购制度.pdf",
        "fileSize": "1.2MB",
        "fileType": "PDF文件",
        "category": "制度",
        "tag": "采购类",
        "description": "采购制度",
        "applicableScope": "procurement",
        "status": "启用",
        "version": 1,
        "uploadUserId": 10001,
        "uploadUserName": "Alice",
        "uploadTime": "2026-08-06 12:30:00",
        "updateTime": "2026-08-06 12:30:00"
      }
    ]
  },
  "timestamp": 1786019400123
}
```

下载和预览不是 `Result` JSON。前端使用响应体的二进制内容和 `Content-Disposition`；不要把数据库里的 `filePath` 拼成浏览器 URL。

### 6.2 Project

Controller 基础路径：`/api/projects`。

| 方法 | 路径 | 请求 | 返回 |
|---|---|---|---|
| POST | `/api/projects` | JSON `ProjectDTO`；`projectName` 非空 | `Result<ProjectVO>` |
| PUT | `/api/projects` | JSON `ProjectDTO`；`id` 必填 | `Result<ProjectVO>`；不存在 404 |
| DELETE | `/api/projects/{id}` | 无请求体 | 成功时 `data` 为空 |
| GET | `/api/projects` | 无必填 query | `Result<ProjectVO[]>`，包含聚合信息 |
| GET | `/api/projects/my` | 无必填 query | `Result<ProjectVO[]>`，当前用户项目 |

`ProjectVO` 字段：`id`、`userId`、`projectName`、`supplierName`、`parseStatus`、`latestVersion`、`createTime`、`updateTime`、`fileCategory`、`auditResult`、`tenders`。

`tenders` 的 item 是 `TenderWithAuditVO`，字段只有 `tender`、`auditTask`、`auditReport`。这三个字段目前是 Java entity，不是冻结的前端 VO；页面按可空对象处理。

### 6.3 Tender

Controller 基础路径：`/api/bid-documents`。

| 方法 | 路径 | 请求 | 返回 |
|---|---|---|---|
| POST | `/api/bid-documents/upload` | multipart：`file` 必填，其他字段绑定 `TenderDTO` | `Result<TenderVO>` |
| GET | `/api/bid-documents` | query 绑定 `TenderPageQueryDTO` | `Result<PageResult>`，`data` 为 `total`、`records` |
| GET | `/api/bid-documents/stats` | query 绑定 `TenderPageQueryDTO` | `Result<TenderStatsVO>` |
| GET | `/api/bid-documents/{id}` | 无请求体 | `Result<TenderVO>` |
| GET | `/api/bid-documents/project/{projectId}/versions` | 无请求体 | `Result<TenderVO[]>` |
| GET | `/api/bid-documents/projects` | 无请求体 | `Result<TenderProjectVO[]>` |
| DELETE | `/api/bid-documents/{id}` | 无请求体 | 成功时 `data` 为空 |
| GET | `/api/bid-documents/{id}/download` | 无请求体 | PDF/Word 预览或原文件二进制 |

`TenderVO` 字段：`id`、`fileName`、`filePath`、`fileSize`、`fileType`、`fileCategory`、`bidName`、`supplierName`、`budgetAmount`、`pageCount`、`parseStatus`、`uploadUserId`、`uploadTime`、`version`、`projectId`、`auditorName`、`auditResult`。

`TenderProjectVO` 字段：`projectId`、`projectName`、`createTime`、`creatorName`、`latestVersion`、`supplierName`、`fileCategory`、`auditorName`。`TenderStatsVO` 字段：`allCount`、`unreviewedCount`、`completedCount`。`filePath` 只能当元数据，下载必须走 `/download`。

### 6.4 AuditTask

Controller 基础路径：`/api/audit-tasks`。

| 方法 | 路径 | 请求 | 返回 |
|---|---|---|---|
| POST | `/api/audit-tasks` | JSON `CreateAuditTaskRequest` | `Result<AuditTaskCreateVO>` |
| GET | `/api/audit-tasks/{taskId}` | 无请求体 | `Result<AuditTaskStatusVO>` |
| POST | `/api/audit-tasks/{taskId}/recover` | 无请求体 | `Result<boolean>` |
| GET | `/api/audit-tasks/{taskId}/result` | query：`page` 默认 1、`size` 默认 20、`sinceIssueNo` 可选 | `Result<ResultVO>` |
| GET | `/api/audit-tasks/{taskId}/stream` | `Accept: text/event-stream`、Bearer、`Last-Event-ID` 可选 | SSE，不是 `Result` |
| GET | `/api/audit-tasks/count-audit` | 无必填 query | `Result<Map<string, number>>` |
| GET | `/api/audit-tasks/{taskId}/blocks` | query：`ids` 必填 | `Result<RustBlockBBoxResponse[]>` |
| POST | `/api/audit-tasks/callback` | query：`taskId`；原始 body | 后端回调，不是浏览器页面接口 |

创建响应只有 `taskId`。状态响应字段：`taskId`、`status`、`stage`、`progress`、`issueCount`、`failedStages`、`totalFileCount`、`pendingFileCount`、`processingFileCount`、`failedFileCount`。

`blocks[]` 字段：`blockId`、`page`（0-based）、`pageWidth`、`bbox`；`bbox` 包含 `x0`、`top`、`x1`、`bottom`，坐标单位是 PDF points。

结果响应 `ResultVO` 字段：`taskId`、`auditResult`、`summary`、`routingSummary`、`graphSnapshot`、`issues`。`summary` 和 `routingSummary` 的字段是 `totalIssues`、`critical`、`high`、`medium`、`low`、`info`、`totalClauses`、`agentClauseCounts`、`legalVerifyCount`、`blindSpotFindings`。

`issues[]` 字段：`issueNo`、`riskId`、`severity`、`isCritical`、`criticalReason`、`category`、`agentName`、`description`、`location`、`suggestion`、`reference`、`sourceQuote`、`legalBasis`、`caseRefs`、`confidence`、`anchorQuote`、`anchorPage`、`anchorSection`、`anchorTokens`、`anchorCharsRange`、`noRisk`、`initialTier`、`finalTier`、`tierEscalated`、`truncated`、`clauseIds`、`blockIds`、`citations`、`suggestedAgent`、`agent`。`location` 是 `pageNumber`、`sectionName`、`context`；`citations[]` 是 `title`、`url`、`siteName`；`suggestedAgent` 是 `agentName`、`agentPrompt`、`sectionKeywords`、`reason`。

审核 SSE envelope：

```json
{
  "schema_version": 1,
  "event_id": "1002",
  "event": "progress",
  "tenant_id": "20001",
  "task_id": "task-01J9J2J8F0M7Y8B6J5G4T3R2Q1",
  "occurred_at": "2026-08-06T13:01:00.123Z",
  "data": {
    "stage": "legal",
    "percent": 42,
    "message": "正在审查"
  }
}
```

连接路径是 `GET /api/audit-tasks/{taskId}/stream`，必须带 Bearer；`event_id` 同时是 SSE 的 `id` 和 JSON 字段。断线用相同 token 和 `Last-Event-ID` 重连；切租户或会话失效后删除旧游标，不能只改 URL 中的租户值。

## 7. tenant_id 的边界

前端可以把服务端返回的 `tenant_id` 作为字符串显示、做路由参数和缓存 key，也可以通过切租户接口提交用户明确选择的目标租户。除此之外：

- 禁止发送 `X-Tenant-Id`，或把它当成授权 Header。
- 禁止给业务请求额外塞 `tenant_id` query/body 来指定数据域。
- 禁止把本地保存的 `tenant_id`、`role`、`permissions` 当成授权来源。
- 禁止只修改 URL 中的租户、任务、文件或文档 ID 来访问另一个租户。
- 不用 `user_id` 或 `uploadUserId` 代替租户过滤。

服务端决定 CurrentTenant；不存在或不属于当前租户的资源统一按 404 处理，前端不要通过错误差异探测其他租户是否存在。

## 8. 统一错误处理

所有请求先解析 `response.status`、`body.data.error_code` 和 `body.data.request_id`，机器分支只看 HTTP 状态和 `error_code`：

| HTTP / error_code | 统一动作 |
|---|---|
| 401 `AUTH_REQUIRED`、`AUTH_INVALID` | 非认证请求只触发一次单飞 refresh；refresh 也失败时清空认证、租户缓存并跳转登录。 |
| 401 `TENANT_SESSION_STALE` | 不重放旧请求。若切租户已有新会话，等待新会话后用新 token 重试一次；否则 refresh 一次。SSE 删除旧游标再连。 |
| 403 `TENANT_ROLE_FORBIDDEN`、`TENANT_OWNER_REQUIRED` | 保留会话，不重试；隐藏或禁用无权限动作。 |
| 403 `TENANT_CONTEXT_INVALID` | 停止操作并重新读取会话，不自行补 Header 或 tenant_id。 |
| 404 `TENANT_NOT_FOUND` | 重新拉取租户列表，撤销本地候选选择。 |
| 404 `RESOURCE_NOT_FOUND` | 清除资源或列表项缓存，显示不可见/不存在；不换 ID 重试。 |
| 404 `TENANT_MEMBER_NOT_FOUND` | 刷新成员列表，不把它当成创建成员信号。 |
| 400 `TENANT_REQUIRED` | 不重试业务请求；有 ACTIVE 租户就走切租户，没有则显示创建租户或无租户状态。 |

## 9. axios / fetch 拦截器伪代码

### 9.1 axios

```ts
const raw = axios.create({ baseURL: "/api" });
const api = axios.create({ baseURL: "/api" });
let refreshInFlight: Promise<AuthState> | null = null;

api.interceptors.request.use((config) => {
  const { token } = authStore.getState();
  if (token) {
    config.headers = config.headers ?? {};
    config.headers.Authorization = `Bearer ${token}`;
  }
  // 不设置 X-Tenant-Id。
  return config;
});

async function refreshOnce() {
  if (!refreshInFlight) {
    const token = authStore.getState().token;
    if (!token) throw new Error("AUTH_REQUIRED");
    refreshInFlight = raw.post("/auth/refresh", null, {
      headers: { Authorization: `Bearer ${token}` },
    }).then((res) => {
      const session = res.data.data as AuthState;
      authStore.replace(session);
      return session;
    }).finally(() => { refreshInFlight = null; });
  }
  return refreshInFlight;
}

api.interceptors.response.use(undefined, async (error) => {
  const config = error.config ?? {};
  const status = error.response?.status;
  const code = error.response?.data?.data?.error_code;
  const isAuthCall = (config.url ?? "").includes("/auth/");
  const canRefresh = status === 401 &&
    ["AUTH_REQUIRED", "AUTH_INVALID", "TENANT_SESSION_STALE"].includes(code) &&
    !config._authRetried && !isAuthCall;
  if (canRefresh) {
    config._authRetried = true;
    await refreshOnce();
    return api.request(config);
  }
  handleTenantError(status, code, error.response?.data?.data?.request_id);
  throw error;
});
```

`raw` 不挂同一个 response 拦截器，避免 refresh 自己再次触发 refresh。生产代码还要用 AbortController 或请求代数，防止旧租户的慢响应覆盖新租户页面。

### 9.2 fetch

```ts
async function fetchApi(input: RequestInfo, init: RequestInit = {}, retried = false) {
  const headers = new Headers(init.headers);
  const token = authStore.getState().token;
  if (token) headers.set("Authorization", `Bearer ${token}`);
  const response = await fetch(input, { ...init, headers });
  if (response.ok) return response;
  const body = await response.clone().json().catch(() => null);
  const code = body?.data?.error_code;
  const isRefresh = String(input).endsWith("/auth/refresh");
  if (response.status === 401 && !retried && !isRefresh &&
      ["AUTH_REQUIRED", "AUTH_INVALID", "TENANT_SESSION_STALE"].includes(code)) {
    await refreshOnce();
    return fetchApi(input, init, true);
  }
  handleTenantError(response.status, code, body?.data?.request_id);
  throw new ApiError(response.status, code, body);
}
```

### 9.3 切租户、清缓存和重连

```ts
async function switchTenant(nextTenantId: string) {
  const previous = authStore.getState().current_tenant?.tenant_id;
  sseManager.closeAll();
  queryClient.cancelQueries({ predicate: (q) => q.queryKey[1] === previous });
  queryClient.removeQueries({ predicate: (q) => q.queryKey[1] === previous });
  lastEventIdStore.remove(previous);

  const response = await api.post("/auth/switch-tenant", {
    tenant_id: nextTenantId,
  });
  authStore.replace(response.data.data);
  queryClient.invalidateQueries({ queryKey: ["tenant", nextTenantId] });
  // 新 SSE 使用新 token，不带旧 Last-Event-ID。
  sseManager.connectForCurrentTenant();
}
```

部署环境改用冻结契约路径时，只替换 adapter 为 `POST /api/tenants/{tenantId}/switch`；页面层不要同时维护两套切租户逻辑。切换期间的旧响应不能写入新租户状态。

## 10. 页面最小改造清单

- 登录页：登录成功后一次性写入 AuthSession；`current_tenant` 为空时进入创建/选择流程。
- 全局 Header：候选项来自 `tenants`；切换时关闭 SSE、清理旧缓存，收到新会话后刷新页面数据。
- 租户页：接入列表、创建和当前租户；创建请求不发送 `owner_user_id`、`status`、`version`、`tenant_id`。
- 成员页：使用当前租户 ID 拼成员路径，缓存 key 带租户；不通过 query/body 选择另一个租户。
- KnowledgeFile 页：适配 `records` 和 camelCase；上传、下载、预览走后端路径，不使用 `filePath` 直连。
- Project/Tender 页：列表、统计、版本和详情使用各自旧 VO 字段；所有缓存带租户命名空间。
- AuditTask 页：状态、结果和 blocks 带当前 token；SSE 处理 envelope，切租户删除旧 `Last-Event-ID`。
- 所有页面：`permissions` 只用于显隐，真正提交后仍按 403 处理。
- 退出：调用 `/api/auth/logout` 后清空内存和 `sessionStorage` 中的认证状态、租户缓存、SSE 和任务游标。

## 11. 联调 smoke checklist

- [ ] 登录：检查 token、token_type、expires_in、session_version、user_info、current_tenant、tenants。
- [ ] 刷新：旧 token 不再用于后续请求，AuthSession 原子替换。
- [ ] 退出：清空 token、当前租户、租户列表和缓存；请求无 body。
- [ ] 多租户用户：ID 按字符串保存；切换后使用新 token，旧请求不会循环重试。
- [ ] 无 ACTIVE 租户：业务请求得到 `400 TENANT_REQUIRED`，页面不猜 tenant_id。
- [ ] 租户创建：只发允许字段，成功后重新读取 AuthSession 或租户列表。
- [ ] 成员列表：验证 `page`、`size`、`total`、`items` 和成员字段；非成员得到 404。
- [ ] A/B 两个租户：KnowledgeFile、Project、Tender、AuditTask 的列表、详情、下载、预览、结果和 SSE 互不可见。
- [ ] 权限不足：按 403 处理，不通过换 ID 或重复提交绕过。
- [ ] SSE：断线用 Last-Event-ID 重连；切租户关闭旧连接、删旧游标，新连接从空游标开始。
- [ ] 请求审计：浏览器请求没有 X-Tenant-Id；错误日志保留 request_id。

## 12. 尚未完成的后端能力和残余风险

1. M3 的租户列表、创建、当前租户、成员列表和契约切租户路径仍在主线程验收；当前基线可直接调用的切租户路径仍是 `/api/auth/switch-tenant`。
2. M4 需要把 tenant_id 过滤和服务端填充落到 Service、Mapper、文件下载/预览、异步任务、缓存和审核结果路径，不能只在 Controller 加参数。
3. 旧业务接口返回 camelCase VO 和 `PageResult.records`，tenant-v1 使用 snake_case 和 `items`；前端 adapter 需要按接口区分。
4. `/api/tenants/current` 的 `session_version` 说明与 OpenAPI/VO 不一致；`user_info.real_name` 也要用原始响应确认，前端不要猜测补齐。
5. OpenAPI 未列出当前 M2 的 `/api/auth/logout`，也未冻结旧业务资源接口的请求模型；联调以实际部署 Controller 和后端确认版本为准。
6. M5 的 Java-Rust 内部签名、SSE 旧会话关闭、回放边界和重放保护尚待验收；浏览器不参与 HMAC，也不能直接调用 Rust。

在 M3/M4/M5 验收完成前，前端可以按本文实现状态管理、错误处理和页面 adapter，但不要把 Pending 接口或旧资源接口的当前可达性当作租户安全完成的证明。
